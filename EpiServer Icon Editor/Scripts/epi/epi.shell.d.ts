declare module "epi/shell/widget/_ValueRequiredMixin" {
    var exp: epi.shell.widget._ValueRequiredMixin;
    export = exp;
}

declare module "epi/shell/widget/dialog/Dialog" {
    var exp: epi.shell.widget.dialog.Dialog;
    export = exp;
}